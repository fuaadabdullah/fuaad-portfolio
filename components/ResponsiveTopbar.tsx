import React from "react";
import Navbar from "./Navbar";

/**
 * ResponsiveTopbar composes `Navbar` for page-level application topbar with
 * responsive behavior already handled by the inner component.
 */
export function ResponsiveTopbar() {
  return <Navbar />;
}

export default ResponsiveTopbar;
