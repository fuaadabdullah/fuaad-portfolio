import React from "react";
import Navbar from "./Navbar";

/**
 * Compound Nav/Topbar component with responsive behavior.
 * Uses Navbar and adds mobile menu toggle (hamburger) for small screens.
 */
export function ResponsiveTopbar() {
  // TODO: Implement hamburger menu for mobile
  return <Navbar />;
}

export default ResponsiveTopbar;
